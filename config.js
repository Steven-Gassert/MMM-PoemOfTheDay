const config = {
	textLimit: 1000,
	lineLimit: 15,
	enableLanguageDetection: false,
	// for a full list of supported languages see https://ws.detectlanguage.com/0.2/languages
	languageSet: ["en", "fr", "es", "it"]
};

module.exports = config;
